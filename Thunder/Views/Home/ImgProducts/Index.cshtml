@model IEnumerable<Thunder.Models.ImgProduct>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ImgProduct1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product.ProduceCode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ImgProduct1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product.ProduceCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ImgId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ImgId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ImgId })
        </td>
    </tr>
}

</table>
